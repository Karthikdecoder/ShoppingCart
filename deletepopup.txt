@model IEnumerable<ShoppingCartWeb.Models.Dto.StateMasterDTO>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    @(ViewContext.RouteData.Values["Controller"].ToString() == "StateMaster" && ViewContext.RouteData.Values["Action"].ToString() == "IndexStateMaster" ? "active" : "")
}


<br />
<div class="row pb-2">
    <div class="col">
        <h1 class="text-dark">States</h1>
    </div>

    <div class="col-2 text-start pt-1 justify-content-start">
        <a asp-controller="StateMaster" asp-action="CreateStateMaster" class="btn btn-primary btn-sm ms-2">
            <div class="d-flex align-items-center">
                <span class="d-inline-block align-middle me-1" style="font-size: 24px;">Create</span>
                <!-- Increase the font-size value as needed -->
                <i class="bi bi-plus-square-fill ml-2" style="font-size: 21px;"></i>
                <!-- Increase the font-size value as needed -->
            </div>
        </a>
    </div>
</div>

<div class="container p-4 border mt-4">

    <div class="table-responsive">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>
                        State Id
                    </th>
                    <th>
                        State Name
                    </th>
                    <th>
                        Country Name
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.StateId</td>
                        <td>@item.StateName</td>
                        <td style="white-space: nowrap;"> @item.CountryMaster.CountryName</td>

                        <td>
                            <div class="btn-group" role="group">
                                <a asp-controller="StateMaster" asp-action="UpdateStateMaster" asp-route-stateId="@item.StateId" class="btn btn-success button-spacing"><i class="bi bi-pencil-square"></i></a>
                                <button type="button" class="btn btn-danger ml-2" data-toggle="modal" data-target="#deleteConfirmationModal" data-state-id="@item.StateId">
                                    <i class="bi bi-trash-fill"></i>
                                </button>
                            </div>

                            <!-- Delete Confirmation Modal -->
                            <div class="modal fade" id="deleteConfirmationModal" tabindex="-1" role="dialog" aria-labelledby="deleteConfirmationModalLabel" aria-hidden="true">
                                <div class="modal-dialog modal-dialog-centered" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="deleteConfirmationModalLabel">Confirm Deletion</h5>
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                                <span aria-hidden="true">&times;</span>
                                            </button>
                                        </div>
                                        <div class="modal-body">
                                            Are you sure you want to delete this item?
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                                            <button type="button" class="btn btn-danger" id="confirmDelete">Delete</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <script>
            $(document).ready(function () {
                $('#deleteConfirmationModal').on('show.bs.modal', function (event) {
                    var button = $(event.relatedTarget); // Button that triggered the modal
                    var stateId = button.data('stateId'); // Extract info from data-* attributes
                    var modal = $(this); // Modal that triggered the event

                    // Update the Delete button's click event
                    modal.find('#confirmDelete').off('click').on('click', function () {
                        alert(stateId);
                        $.ajax({
                            type: 'POST', // Or 'GET' depending on your controller action
                            url: '@Url.Action("RemoveStateMaster", "StateMaster")', // Update with your controller and action
                            data: { id: stateId }, // Pass the stateId to the action
                            dataType: 'json',
                            success: function (data) {
                                alert("Yes");
                                // Handle success, like refreshing the page or updating the UI
                                // For example, you can reload the page: window.location.reload();
                                location.reload(true);
                            },
                            error: function () {
                                alert("No");
                                // Handle error, if needed
                                location.reload(true);
                            }
                        });

                        modal.modal('hide'); // Close the modal
                    });
                });
            });
        </script>

    </div>
</div>


